spring:
  datasource:
    url: jdbc:mysql://192.168.1.10:3306/vue_shop?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    username: root
    password: root
mybatis:
  mapper-locations: classpath*:mapper/*Mapper.xml
  configuration:
    cache-enabled: true
    map-underscore-to-camel-case: true

logging:
  level:
    com.huangjicheng.top.vepdao.mapper: debug #打印sql